// import { selectUsersNavigationDomain } from '../selectors';

describe('selectUsersNavigationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
