/*
 * LivelyMobileDocuments Messages
 *
 * This contains all the text for the LivelyMobileDocuments container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LivelyMobileDocuments';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LivelyMobileDocuments container!',
  },
});
