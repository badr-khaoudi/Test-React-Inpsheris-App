// import { selectEditMyProfileDomain } from '../selectors';

describe('selectEditMyProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
