/*
 * EditMyProfile Messages
 *
 * This contains all the text for the EditMyProfile container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditMyProfile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditMyProfile container!',
  },
});
