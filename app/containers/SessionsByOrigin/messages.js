/*
 * SessionsByOrigin Messages
 *
 * This contains all the text for the SessionsByOrigin container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SessionsByOrigin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SessionsByOrigin container!',
  },
});
