/*
 * MyDraft Messages
 *
 * This contains all the text for the MyDraft container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MyDraft';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MyDraft container!',
  },
});
