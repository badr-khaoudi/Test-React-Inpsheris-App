/*
 * CommentArea Messages
 *
 * This contains all the text for the CommentArea container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CommentArea';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CommentArea container!',
  },
});
