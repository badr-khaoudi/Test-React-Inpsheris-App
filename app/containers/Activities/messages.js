/*
 * Activities Messages
 *
 * This contains all the text for the Activities container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Activities';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Activities container!',
  },
});
