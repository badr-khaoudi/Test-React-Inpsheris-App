/*
 * DocumentBlock Messages
 *
 * This contains all the text for the DocumentBlock container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DocumentBlock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentBlock container!',
  },
});
