// import { selectDocumentBlockDomain } from '../selectors';

describe('selectDocumentBlockDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
