/*
 * CreateCarousel Messages
 *
 * This contains all the text for the CreateCarousel container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateCarousel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateCarousel container!',
  },
});
