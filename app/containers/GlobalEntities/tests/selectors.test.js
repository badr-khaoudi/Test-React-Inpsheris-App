// import { selectGlobalEntitiesDomain } from '../selectors';

describe('selectGlobalEntitiesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
