/*
 * SocialWall Messages
 *
 * This contains all the text for the SocialWall container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SocialWall';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SocialWall container!',
  },
});
