/*
 * ImageCrop Messages
 *
 * This contains all the text for the ImageCrop container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ImageCrop';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ImageCrop container!',
  },
});
