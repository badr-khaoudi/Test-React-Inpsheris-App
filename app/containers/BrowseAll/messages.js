/*
 * BrowseAll Messages
 *
 * This contains all the text for the BrowseAll container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BrowseAll';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BrowseAll container!',
  },
});
