// import { selectCarouselItemDomain } from '../selectors';

describe('selectCarouselItemDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
