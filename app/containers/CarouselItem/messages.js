/*
 * CarouselItem Messages
 *
 * This contains all the text for the CarouselItem container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CarouselItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CarouselItem container!',
  },
});
