/*
 * UserLiked Messages
 *
 * This contains all the text for the UserLiked container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserLiked';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserLiked container!',
  },
});
