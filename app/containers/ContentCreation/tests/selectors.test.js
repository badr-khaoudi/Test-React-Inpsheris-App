// import { selectContentCreationDomain } from '../selectors';

describe('selectContentCreationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
