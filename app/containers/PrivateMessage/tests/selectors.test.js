// import { selectPrivateMessageDomain } from '../selectors';

describe('selectPrivateMessageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
