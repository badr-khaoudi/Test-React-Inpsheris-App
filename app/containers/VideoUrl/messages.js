/*
 * VideoUrl Messages
 *
 * This contains all the text for the VideoUrl container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VideoUrl';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VideoUrl container!',
  },
});
