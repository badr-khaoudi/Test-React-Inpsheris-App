// import { selectSearchDirectoryDomain } from '../selectors';

describe('selectSearchDirectoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
