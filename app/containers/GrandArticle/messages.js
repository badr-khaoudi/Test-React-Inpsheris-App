/*
 * GrandArticle Messages
 *
 * This contains all the text for the GrandArticle container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GrandArticle';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GrandArticle container!',
  },
});
