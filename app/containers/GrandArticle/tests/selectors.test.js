// import { selectGrandArticleDomain } from '../selectors';

describe('selectGrandArticleDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
