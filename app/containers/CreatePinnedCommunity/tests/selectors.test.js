// import { selectCreatePinnedCommunityDomain } from '../selectors';

describe('selectCreatePinnedCommunityDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
