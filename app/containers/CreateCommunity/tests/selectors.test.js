// import { selectCreateCommunityDomain } from '../selectors';

describe('selectCreateCommunityDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
