// import { selectLivelyTransferDomain } from '../selectors';

describe('selectLivelyTransferDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
