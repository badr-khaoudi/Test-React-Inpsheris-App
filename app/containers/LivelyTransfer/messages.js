/*
 * LivelyTransfer Messages
 *
 * This contains all the text for the LivelyTransfer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LivelyTransfer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LivelyTransfer container!',
  },
});
