/*
 * Directory Messages
 *
 * This contains all the text for the Directory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Directory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Directory container!',
  },
});
