// import { selectDirectoryPrivateMessageDomain } from '../selectors';

describe('selectDirectoryPrivateMessageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
