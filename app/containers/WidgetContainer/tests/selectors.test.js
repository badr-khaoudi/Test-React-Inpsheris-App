// import { selectWidgetContainerDomain } from '../selectors';

describe('selectWidgetContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
