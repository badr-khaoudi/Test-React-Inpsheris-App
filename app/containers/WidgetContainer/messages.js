/*
 * WidgetContainer Messages
 *
 * This contains all the text for the WidgetContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WidgetContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WidgetContainer container!',
  },
});
