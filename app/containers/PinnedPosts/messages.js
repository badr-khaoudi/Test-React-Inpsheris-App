/*
 * PinnedPosts Messages
 *
 * This contains all the text for the PinnedPosts container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PinnedPosts';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PinnedPosts container!',
  },
});
