/*
 * UserAutocomplete Messages
 *
 * This contains all the text for the UserAutocomplete container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserAutocomplete';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserAutocomplete container!',
  },
});
