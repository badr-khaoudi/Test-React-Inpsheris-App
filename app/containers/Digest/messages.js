/*
 * Digest Messages
 *
 * This contains all the text for the Digest container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Digest';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Digest container!',
  },
});
