/*
 * DigestList Messages
 *
 * This contains all the text for the DigestList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DigestList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DigestList container!',
  },
});
