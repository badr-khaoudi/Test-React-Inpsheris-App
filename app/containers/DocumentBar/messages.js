/*
 * DocumentBar Messages
 *
 * This contains all the text for the DocumentBar container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DocumentBar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentBar container!',
  },
});
