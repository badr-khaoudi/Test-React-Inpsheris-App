/*
 * WidgetPage Messages
 *
 * This contains all the text for the WidgetPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WidgetPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WidgetPage container!',
  },
});
