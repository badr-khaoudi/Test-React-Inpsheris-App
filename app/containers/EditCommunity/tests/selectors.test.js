// import { selectEditCommunityDomain } from '../selectors';

describe('selectEditCommunityDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
