// import { selectCommunityStatisticsDomain } from '../selectors';

describe('selectCommunityStatisticsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
