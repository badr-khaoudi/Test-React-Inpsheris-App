/*
 * EditAbout Messages
 *
 * This contains all the text for the EditAbout container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditAbout';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditAbout container!',
  },
});
