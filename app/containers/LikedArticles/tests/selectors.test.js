// import { selectLikedArticlesDomain } from '../selectors';

describe('selectLikedArticlesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
