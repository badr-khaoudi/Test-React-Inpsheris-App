/*
 * LikedArticles Messages
 *
 * This contains all the text for the LikedArticles container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LikedArticles';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LikedArticles container!',
  },
});
