/*
 * CommunityTab Messages
 *
 * This contains all the text for the CommunityTab container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CommunityTab';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CommunityTab container!',
  },
});
