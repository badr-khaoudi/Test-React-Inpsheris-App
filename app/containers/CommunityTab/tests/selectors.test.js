// import { selectCommunityTabDomain } from '../selectors';

describe('selectCommunityTabDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
