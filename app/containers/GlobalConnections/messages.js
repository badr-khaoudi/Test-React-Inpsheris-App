/*
 * GlobalConnections Messages
 *
 * This contains all the text for the GlobalConnections container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GlobalConnections';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GlobalConnections container!',
  },
});
