// import { selectWidgetManagerDomain } from '../selectors';

describe('selectWidgetManagerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
