/*
 * WidgetManager Messages
 *
 * This contains all the text for the WidgetManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WidgetManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WidgetManager container!',
  },
});
