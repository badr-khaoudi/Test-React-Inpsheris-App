// import { selectCreateDigestDomain } from '../selectors';

describe('selectCreateDigestDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
