/*
 * CreateDigest Messages
 *
 * This contains all the text for the CreateDigest container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateDigest';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateDigest container!',
  },
});
