/*
 * CommunityHome Messages
 *
 * This contains all the text for the CommunityHome container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CommunityHome';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CommunityHome container!',
  },
});
