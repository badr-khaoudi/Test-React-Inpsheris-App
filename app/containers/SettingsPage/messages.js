/*
 * LivelyMobileSettings Messages
 *
 * This contains all the text for the LivelyMobileSettings container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LivelyMobileSettings';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LivelyMobileSettings container!',
  },
});
