/*
 * LivelyMobileApplicationLinks Messages
 *
 * This contains all the text for the LivelyMobileApplicationLinks container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LivelyMobileApplicationLinks';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LivelyMobileApplicationLinks container!',
  },
});
