// import { selectMoveDocumentDomain } from '../selectors';

describe('selectMoveDocumentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
