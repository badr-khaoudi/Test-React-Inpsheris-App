/*
 * MoveDocument Messages
 *
 * This contains all the text for the MoveDocument container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MoveDocument';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MoveDocument container!',
  },
});
