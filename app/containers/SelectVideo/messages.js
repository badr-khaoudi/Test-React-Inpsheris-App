/*
 * SelectVideo Messages
 *
 * This contains all the text for the SelectVideo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SelectVideo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SelectVideo container!',
  },
});
