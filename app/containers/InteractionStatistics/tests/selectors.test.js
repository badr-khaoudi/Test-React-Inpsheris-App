// import { selectInteractionStatisticsDomain } from '../selectors';

describe('selectInteractionStatisticsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
