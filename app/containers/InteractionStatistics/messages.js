/*
 * InteractionStatistics Messages
 *
 * This contains all the text for the InteractionStatistics container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.InteractionStatistics';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InteractionStatistics container!',
  },
});
