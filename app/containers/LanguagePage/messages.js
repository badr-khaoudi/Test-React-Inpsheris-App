/*
 * LivelyMobileLanguage Messages
 *
 * This contains all the text for the LivelyMobileLanguage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LivelyMobileLanguage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LivelyMobileLanguage container!',
  },
});
