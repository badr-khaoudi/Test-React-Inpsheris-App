/*
 * MyPublish Messages
 *
 * This contains all the text for the MyPublish container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MyPublish';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MyPublish container!',
  },
});
