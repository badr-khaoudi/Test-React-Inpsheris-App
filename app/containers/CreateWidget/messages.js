/*
 * CreateWidget Messages
 *
 * This contains all the text for the CreateWidget container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateWidget';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateWidget container!',
  },
});
