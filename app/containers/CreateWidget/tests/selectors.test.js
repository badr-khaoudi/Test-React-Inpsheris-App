// import { selectCreateWidgetDomain } from '../selectors';

describe('selectCreateWidgetDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
