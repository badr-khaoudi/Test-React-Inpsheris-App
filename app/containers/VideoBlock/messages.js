/*
 * VideoBlock Messages
 *
 * This contains all the text for the VideoBlock container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VideoBlock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VideoBlock container!',
  },
});
