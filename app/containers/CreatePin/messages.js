/*
 * CreatePin Messages
 *
 * This contains all the text for the CreatePin container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreatePin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreatePin container!',
  },
});
