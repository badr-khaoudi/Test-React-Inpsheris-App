/*
 * WidgetVideoViewedByUser Messages
 *
 * This contains all the text for the WidgetVideoViewedByUser container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WidgetVideoViewedByUser';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WidgetVideoViewedByUser container!',
  },
});
