// import { selectWidgetVideoViewedByUserDomain } from '../selectors';

describe('selectWidgetVideoViewedByUserDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
