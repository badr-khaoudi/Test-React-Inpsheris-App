// import { selectFlexdeskReportDomain } from '../selectors';

describe('selectFlexdeskReportDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
