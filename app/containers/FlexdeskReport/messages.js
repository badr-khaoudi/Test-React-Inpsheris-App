/*
 * FlexdeskReport Messages
 *
 * This contains all the text for the FlexdeskReport container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FlexdeskReport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FlexdeskReport container!',
  },
});
