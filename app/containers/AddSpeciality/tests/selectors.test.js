// import { selectAddSpecialityDomain } from '../selectors';

describe('selectAddSpecialityDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
