/*
 * DriveTree Messages
 *
 * This contains all the text for the DriveTree component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DriveTree';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DriveTree component!',
  },
});
