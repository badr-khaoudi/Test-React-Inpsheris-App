/*
 * DeleteDialog Messages
 *
 * This contains all the text for the DeleteDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DeleteDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeleteDialog component!',
  },
});
