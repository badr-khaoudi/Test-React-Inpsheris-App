/*
 * SortableList Messages
 *
 * This contains all the text for the SortableList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SortableList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SortableList component!',
  },
});
