/*
 * DocumentBarDialog Messages
 *
 * This contains all the text for the DocumentBarDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DocumentBarDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentBarDialog component!',
  },
});
