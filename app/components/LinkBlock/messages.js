/*
 * LinkBlock Messages
 *
 * This contains all the text for the LinkBlock component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LinkBlock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LinkBlock component!',
  },
});
