/*
 * Widgets Messages
 *
 * This contains all the text for the Widgets component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Widgets';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Widgets component!',
  },
});
