/*
 * LivelyMenuList Messages
 *
 * This contains all the text for the LivelyMenuList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LivelyMenuList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LivelyMenuList component!',
  },
});
