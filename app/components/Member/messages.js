/*
 * Member Messages
 *
 * This contains all the text for the Member component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Member';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Member component!',
  },
});
